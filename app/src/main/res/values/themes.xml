<resources>
    <!-- Base application theme. -->
    <style name="Theme.Carrot_AOS" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryVariant">@color/light_orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Neutral brand color. -->
        <item name="android:colorBackground">@color/white</item>
        <item name="colorOnBackground">@color/black</item>
        <item name="colorSurface">@color/gray_1</item>
        <item name="colorOnSurface">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?android:colorBackground</item>
        
        <!-- etc -->
        <item name="android:buttonStyle">@style/Widget.Carrot.Button</item>

    </style>
</resources>